---
plan: 14-01
title: "Rename to Infrahaus: dashboard metadata, README, package names"
wave: 1
depends_on: []
files_modified:
  - apps/dashboard/src/app/layout.tsx
  - apps/dashboard/src/components/app-sidebar.tsx
  - apps/dashboard/src/app/login/page.tsx
  - apps/dashboard/src/lib/web3/config.ts
  - apps/dashboard/src/components/providers/web3-provider.tsx
  - apps/dashboard/package.json
  - apps/dashboard/src/app/favicon.ico
  - apps/dashboard/README.md
  - Dockerfile
  - docker-compose.yml
  - .github/workflows/ci.yml
  - apps/dashboard/playwright.config.ts
  - README.md
autonomous: true
requirements:
  - REQ-2.07
---

# Plan 14-01: Rename to Infrahaus: dashboard metadata, README, package names

## Goal

Replace all remaining "LXC Manager" / "LXC Template Manager Dashboard" product name occurrences throughout the monorepo with "Infrahaus". This covers the Next.js app metadata, sidebar and login UI, Web3/RainbowKit configuration, the dashboard package name (with scoped `@infrahaus/dashboard`), all downstream `--filter dashboard` references in Dockerfile/CI/playwright/README, the apps/dashboard README, and the favicon asset.

## must_haves

- [ ] `apps/dashboard/src/app/layout.tsx` metadata `title` is `"Infrahaus"` (with `%s | Infrahaus` template) and `description` is updated
- [ ] Sidebar header in `app-sidebar.tsx` shows "Infrahaus" (not "LXC Manager")
- [ ] Login page `CardTitle` shows "Infrahaus" (not "LXC Manager")
- [ ] `web3/config.ts` `appName` is `"Infrahaus"`
- [ ] `web3-provider.tsx` SIWE statement references "Infrahaus"
- [ ] `apps/dashboard/package.json` `name` is `"@infrahaus/dashboard"`
- [ ] All `--filter dashboard` in Dockerfile, docker-compose.yml, ci.yml, playwright.config.ts, and root README updated to `--filter @infrahaus/dashboard`
- [ ] `apps/dashboard/README.md` heading and description updated to "Infrahaus"
- [ ] `favicon.ico` replaced with a custom Infrahaus branded icon (not the default Next.js placeholder)
- [ ] `npx tsc --noEmit` passes with no new type errors
- [ ] `grep -rn "LXC Manager\|LXC Template Manager" apps/dashboard/src/ apps/dashboard/README.md Dockerfile docker-compose.yml .github/` returns zero results

## Tasks

<task id="14-01-01" type="edit">
<title>Update dashboard metadata in layout.tsx</title>
<file>apps/dashboard/src/app/layout.tsx</file>
<description>
Replace the `metadata` export to use the "Infrahaus" product name with the per-page title template pattern:

```ts
export const metadata: Metadata = {
  title: {
    default: "Infrahaus",
    template: "%s | Infrahaus",
  },
  description: "Self-hosted infrastructure management on Proxmox VE",
};
```

Change `title: "LXC Template Manager Dashboard"` → `title: { default: "Infrahaus", template: "%s | Infrahaus" }`.
Change `description: "Manage and deploy LXC containers on Proxmox VE"` → `description: "Self-hosted infrastructure management on Proxmox VE"`.
</description>
<automated>
grep -n "Infrahaus" /root/projects/infrahaus/apps/dashboard/src/app/layout.tsx
</automated>
</task>

<task id="14-01-02" type="edit">
<title>Update sidebar brand name in app-sidebar.tsx</title>
<file>apps/dashboard/src/components/app-sidebar.tsx</file>
<description>
In the sidebar header, change the primary brand label from "LXC Manager" to "Infrahaus":

```tsx
<span className="font-semibold">Infrahaus</span>
<span className="text-xs">Dashboard</span>
```

The sub-label "Dashboard" is a correct descriptor and should remain unchanged.
</description>
<automated>
grep -n "LXC Manager" /root/projects/infrahaus/apps/dashboard/src/components/app-sidebar.tsx; echo "exit: $?"
</automated>
</task>

<task id="14-01-03" type="edit">
<title>Update login page heading in login/page.tsx</title>
<file>apps/dashboard/src/app/login/page.tsx</file>
<description>
Change the `CardTitle` text from "LXC Manager" to "Infrahaus":

```tsx
<CardTitle className="text-2xl">Infrahaus</CardTitle>
```
</description>
<automated>
grep -n "LXC Manager" /root/projects/infrahaus/apps/dashboard/src/app/login/page.tsx; echo "exit: $?"
</automated>
</task>

<task id="14-01-04" type="edit">
<title>Update RainbowKit appName in web3/config.ts</title>
<file>apps/dashboard/src/lib/web3/config.ts</file>
<description>
Change `appName: "LXC Manager"` to `appName: "Infrahaus"`. This name appears in the WalletConnect pairing UI shown to users in their wallet app.
</description>
<automated>
grep -n "appName" /root/projects/infrahaus/apps/dashboard/src/lib/web3/config.ts
</automated>
</task>

<task id="14-01-05" type="edit">
<title>Update SIWE statement in web3-provider.tsx</title>
<file>apps/dashboard/src/components/providers/web3-provider.tsx</file>
<description>
Change the SIWE statement from `"Sign in to LXC Manager with your Universal Profile."` to `"Sign in to Infrahaus with your Universal Profile."`. This message appears in users' wallet signing prompts for new sessions.
</description>
<automated>
grep -n "LXC Manager" /root/projects/infrahaus/apps/dashboard/src/components/providers/web3-provider.tsx; echo "exit: $?"
</automated>
</task>

<task id="14-01-06" type="edit">
<title>Rename package to @infrahaus/dashboard in package.json</title>
<file>apps/dashboard/package.json</file>
<description>
Change `"name": "dashboard"` to `"name": "@infrahaus/dashboard"`. This is a scoped package name. pnpm's partial filter matching means existing `--filter dashboard` commands would still resolve, but all downstream references will be updated explicitly in the following tasks for clarity.
</description>
<automated>
grep -n '"name"' /root/projects/infrahaus/apps/dashboard/package.json
</automated>
</task>

<task id="14-01-07" type="edit">
<title>Update --filter references in Dockerfile</title>
<file>Dockerfile</file>
<description>
Replace both occurrences of `pnpm --filter dashboard` with `pnpm --filter @infrahaus/dashboard`:

- Line 29: `RUN pnpm --filter dashboard exec prisma generate` → `RUN pnpm --filter @infrahaus/dashboard exec prisma generate`
- Line 30: `RUN pnpm --filter dashboard build` → `RUN pnpm --filter @infrahaus/dashboard build`
</description>
<automated>
grep -n "filter dashboard" /root/projects/infrahaus/Dockerfile; echo "exit: $?"
</automated>
</task>

<task id="14-01-08" type="edit">
<title>Update --filter references in docker-compose.yml</title>
<file>docker-compose.yml</file>
<description>
Replace all occurrences of `pnpm --filter dashboard` with `pnpm --filter @infrahaus/dashboard` in docker-compose.yml.
</description>
<automated>
grep -n "filter dashboard" /root/projects/infrahaus/docker-compose.yml; echo "exit: $?"
</automated>
</task>

<task id="14-01-09" type="edit">
<title>Update --filter references in CI workflow</title>
<file>.github/workflows/ci.yml</file>
<description>
Replace all occurrences of `pnpm --filter dashboard` with `pnpm --filter @infrahaus/dashboard` in the CI workflow file. There are multiple occurrences (lines 109, 112, 169, 172, 175, 181). Use a global replace to catch all instances.
</description>
<automated>
grep -n "filter dashboard" /root/projects/infrahaus/.github/workflows/ci.yml; echo "exit: $?"
</automated>
</task>

<task id="14-01-10" type="edit">
<title>Update --filter reference in playwright.config.ts</title>
<file>apps/dashboard/playwright.config.ts</file>
<description>
Replace `pnpm --filter dashboard dev` with `pnpm --filter @infrahaus/dashboard dev` in the `webServer.command` configuration (line 30).
</description>
<automated>
grep -n "filter dashboard" /root/projects/infrahaus/apps/dashboard/playwright.config.ts; echo "exit: $?"
</automated>
</task>

<task id="14-01-11" type="edit">
<title>Update --filter reference in root README</title>
<file>README.md</file>
<description>
Replace the `pnpm --filter dashboard dev` command in the Quick Start section with `pnpm --filter @infrahaus/dashboard dev`. This is the developer-facing command in the root README (line 43).
</description>
<automated>
grep -n "filter dashboard" /root/projects/infrahaus/README.md; echo "exit: $?"
</automated>
</task>

<task id="14-01-12" type="edit">
<title>Update apps/dashboard README</title>
<file>apps/dashboard/README.md</file>
<description>
Update the README heading and description:

```md
# Infrahaus — Dashboard

Self-hosted infrastructure management on Proxmox VE.
```

Replace the existing `# Dashboard` heading and `LXC Template Manager Dashboard for Proxmox VE.` description line.
</description>
<automated>
grep -n "LXC\|Infrahaus" /root/projects/infrahaus/apps/dashboard/README.md
</automated>
</task>

<task id="14-01-13" type="edit">
<title>Replace favicon with Infrahaus branded icon</title>
<file>apps/dashboard/src/app/favicon.ico</file>
<description>
Replace the default Next.js placeholder favicon with a custom Infrahaus branded icon.

The current file is the standard Next.js default ICO (25.9 KB, 16x16 and 32x32 at 32bpp). Replace it with a minimal Infrahaus favicon using the metadata API approach — add an `icon.svg` file at `apps/dashboard/src/app/icon.svg` with a simple "IH" monogram or infrastructure-themed SVG icon.

Create `apps/dashboard/src/app/icon.svg` with a minimal SVG favicon:

```svg
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 32 32" width="32" height="32">
  <rect width="32" height="32" rx="6" fill="#0f172a"/>
  <text x="50%" y="50%" dominant-baseline="central" text-anchor="middle"
        font-family="system-ui, sans-serif" font-size="14" font-weight="700"
        fill="#f8fafc">IH</text>
</svg>
```

Next.js App Router automatically serves `app/icon.svg` as the favicon via its metadata file convention, providing better quality than the default ICO on modern browsers. The existing `favicon.ico` may remain as a fallback for legacy browsers — no deletion needed.

**Note:** If creating a new file `apps/dashboard/src/app/icon.svg` — use the `create` task type. The `favicon.ico` itself does not need modification.
</description>
<automated>
ls -la /root/projects/infrahaus/apps/dashboard/src/app/icon.svg 2>/dev/null || ls -la /root/projects/infrahaus/apps/dashboard/src/app/favicon.ico
</automated>
</task>

<task id="14-01-14" type="verify">
<title>Verify zero old branding strings remain in production code</title>
<file>apps/dashboard/src/</file>
<description>
Run the full branding audit grep to confirm no "LXC Manager" or "LXC Template Manager" strings remain in any production source file, config, or docs.
</description>
<automated>
grep -rn "LXC Manager\|LXC Template Manager" \
  /root/projects/infrahaus/apps/dashboard/src/ \
  /root/projects/infrahaus/apps/dashboard/README.md \
  /root/projects/infrahaus/README.md \
  /root/projects/infrahaus/Dockerfile \
  /root/projects/infrahaus/docker-compose.yml \
  /root/projects/infrahaus/.github/ \
  --include="*.ts" --include="*.tsx" --include="*.json" \
  --include="*.md" --include="*.yml" --include="*.yaml" 2>/dev/null; echo "grep exit: $? (0 matches = success)"
</automated>
</task>

<task id="14-01-15" type="verify">
<title>TypeScript type-check passes</title>
<file>apps/dashboard/</file>
<description>
Run `npx tsc --noEmit` in the dashboard directory to confirm no type errors were introduced by the branding changes.
</description>
<automated>
cd /root/projects/infrahaus/apps/dashboard && npx tsc --noEmit 2>&1 | tail -5; echo "tsc exit: $?"
</automated>
</task>

## Verification

After all tasks complete, the following automated checks must all pass:

1. **Zero old branding strings:** `grep -rn "LXC Manager\|LXC Template Manager" apps/dashboard/src/ apps/dashboard/README.md README.md Dockerfile docker-compose.yml .github/` returns no output.

2. **Package name updated:** `grep '"name"' apps/dashboard/package.json` outputs `"name": "@infrahaus/dashboard"`.

3. **Metadata updated:** `grep -n "Infrahaus" apps/dashboard/src/app/layout.tsx` shows the new title and description.

4. **No --filter dashboard references remain:** `grep -rn "filter dashboard" Dockerfile docker-compose.yml .github/workflows/ci.yml apps/dashboard/playwright.config.ts README.md` returns no output.

5. **TypeScript passes:** `cd apps/dashboard && npx tsc --noEmit` exits 0.

6. **Favicon asset present:** `ls apps/dashboard/src/app/icon.svg` succeeds (custom Infrahaus SVG favicon exists).

Manual checks (require dev server):
- Browser tab on any dashboard page shows "Infrahaus" or "PageName | Infrahaus"
- `/login` page heading shows "Infrahaus"
- Sidebar header shows "Infrahaus" as the primary brand label
- Favicon in browser tab shows custom IH monogram icon (not the Next.js default)
