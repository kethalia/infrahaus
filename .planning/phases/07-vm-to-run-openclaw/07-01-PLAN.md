---
phase: 07-vm-to-run-openclaw
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - infra/vm/templates/openclaw-desktop/template.conf
  - infra/vm/templates/openclaw-desktop/cloud-init/user-data.yaml
  - infra/vm/templates/openclaw-desktop/create-vm.sh
autonomous: true
requirements:
  - VM-01
  - VM-02

must_haves:
  truths:
    - "Running create-vm.sh on a Proxmox host creates a Debian 13 VM that remains running (not converted to a template)"
    - "Cloud-init bootstraps only: openclaw user, SSH, basic prerequisite packages, and qemu-guest-agent — no desktop software"
    - "After create-vm.sh completes, the VM is running and ready for run-scripts.sh to install desktop software"
  artifacts:
    - path: "infra/vm/templates/openclaw-desktop/template.conf"
      provides: "VM template metadata and env-var-overridable resource defaults"
      contains: "TEMPLATE_APP"
    - path: "infra/vm/templates/openclaw-desktop/cloud-init/user-data.yaml"
      provides: "Minimal cloud-init bootstrap — user, SSH, basic packages only"
      contains: "#cloud-config"
    - path: "infra/vm/templates/openclaw-desktop/create-vm.sh"
      provides: "VM creation wrapper that calls community Debian 13 script then applies cloud-init"
      contains: "debian-13-vm.sh"
  key_links:
    - from: "infra/vm/templates/openclaw-desktop/create-vm.sh"
      to: "infra/vm/templates/openclaw-desktop/template.conf"
      via: "source template.conf at script start"
      pattern: "source.*template\\.conf"
    - from: "infra/vm/templates/openclaw-desktop/create-vm.sh"
      to: "ProxmoxVE community debian-13-vm.sh"
      via: "bash -c subshell (NOT source — community VM scripts are whiptail-interactive)"
      pattern: "debian-13-vm"
    - from: "infra/vm/templates/openclaw-desktop/create-vm.sh"
      to: "infra/vm/templates/openclaw-desktop/cloud-init/user-data.yaml"
      via: "cp to /var/lib/vz/snippets/ then qm set --cicustom"
      pattern: "cicustom"
---

<objective>
Create the VM template directory structure: configuration metadata (template.conf), minimal cloud-init bootstrap (cloud-init/user-data.yaml), and the VM creation wrapper script (create-vm.sh) that uses the ProxmoxVE community Debian 13 VM script as its foundation.

Purpose: Establish the infra/vm/ pattern following the same conventions as infra/lxc/templates/web3-dev/ but adapted for VM-specific concerns (UEFI boot, virtio drivers, cloud-init, desktop resources). Cloud-init is intentionally minimal — all software installation happens via post-install scripts (Plan 02). After create-vm.sh finishes, the VM stays running (never converted to a Proxmox template via `qm template`).

Output: Three files — template.conf (metadata and resource defaults), cloud-init/user-data.yaml (minimal bootstrap), and create-vm.sh (entry point wrapping the community script).
</objective>

<execution_context>
@/root/.claude/get-shit-done/workflows/execute-plan.md
@/root/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-vm-to-run-openclaw/07-CONTEXT.md
@.planning/phases/07-vm-to-run-openclaw/07-RESEARCH.md

# Existing LXC template for naming/pattern reference
@infra/lxc/templates/web3-dev/template.conf
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create template.conf and cloud-init/user-data.yaml</name>
  <files>
    infra/vm/templates/openclaw-desktop/template.conf
    infra/vm/templates/openclaw-desktop/cloud-init/user-data.yaml
  </files>
  <action>
Create `infra/vm/templates/openclaw-desktop/template.conf` following the pattern of `infra/lxc/templates/web3-dev/template.conf` but with VM-specific variables:

```bash
#!/usr/bin/env bash
# OpenClaw Desktop VM Template Configuration

# Template identification
TEMPLATE_APP="OpenClaw Desktop"
TEMPLATE_TAGS="openclaw;desktop;xfce;nodejs;chrome"
TEMPLATE_DESCRIPTION="Desktop VM with XFCE, Chrome, Node.js for running OpenClaw GUI applications"

# Template path (relative to repository root)
TEMPLATE_CONFIG_PATH="infra/vm/templates/openclaw-desktop"

# Resource defaults — desktop-appropriate (higher than server defaults)
TEMPLATE_CPU="${TEMPLATE_CPU:-4}"
TEMPLATE_RAM="${TEMPLATE_RAM:-4096}"
TEMPLATE_DISK="${TEMPLATE_DISK:-32G}"
TEMPLATE_OS="${TEMPLATE_OS:-debian}"
TEMPLATE_VERSION="${TEMPLATE_VERSION:-13}"

# VM-specific settings (no LXC equivalents)
TEMPLATE_MACHINE="${TEMPLATE_MACHINE:-q35}"
TEMPLATE_BIOS="${TEMPLATE_BIOS:-ovmf}"
TEMPLATE_DISPLAY="${TEMPLATE_DISPLAY:-virtio}"
TEMPLATE_CPU_TYPE="${TEMPLATE_CPU_TYPE:-host}"
TEMPLATE_START_VM="${TEMPLATE_START_VM:-yes}"

# User configuration
TEMPLATE_USER="${TEMPLATE_USER:-openclaw}"
TEMPLATE_HOSTNAME="${TEMPLATE_HOSTNAME:-openclaw-desktop}"
```

Create `infra/vm/templates/openclaw-desktop/cloud-init/user-data.yaml` as a MINIMAL bootstrap. This file must NOT install any desktop software, Node.js, Chrome, VNC, or OpenClaw — those all belong in scripts/ (Plan 02).

Cloud-init handles ONLY:
- Create `openclaw` user (UID 1000, groups: sudo,audio,video,render, shell: /bin/bash)
- Set initial password to `openclaw` with `chage -d 0` to force change on first interactive login
- Configure passwordless sudo for the `openclaw` user (needed for run-scripts.sh automation)
- Install minimal prerequisite packages: qemu-guest-agent, openssh-server, curl, wget, ca-certificates, gnupg, git, sudo
- Enable qemu-guest-agent and SSH services

```yaml
#cloud-config
# MINIMAL BOOTSTRAP ONLY — all software (XFCE, Chrome, Node.js, VNC, OpenClaw) is installed
# by post-install scripts in scripts/. Run them via: ./run-scripts.sh <VM_IP>
hostname: openclaw-desktop
manage_etc_hosts: true

users:
  - name: openclaw
    groups: sudo,audio,video,render
    shell: /bin/bash
    sudo: ALL=(ALL) NOPASSWD:ALL
    lock_passwd: false

package_update: true

packages:
  # Bootstrap prerequisites only — NOT desktop software
  - qemu-guest-agent
  - openssh-server
  - curl
  - wget
  - ca-certificates
  - gnupg
  - git
  - sudo

runcmd:
  - echo "openclaw:openclaw" | chpasswd
  - chage -d 0 openclaw
  - systemctl enable --now qemu-guest-agent
  - systemctl enable --now ssh

final_message: "Bootstrap complete after $UPTIME seconds. Run ./run-scripts.sh <VM_IP> to install desktop software."
```

Note: `sudo: ALL=(ALL) NOPASSWD:ALL` is required so run-scripts.sh can execute root-level commands as the openclaw user when root SSH login is disabled. The user can restrict this post-setup.

Create the directory structure: `mkdir -p infra/vm/templates/openclaw-desktop/cloud-init/`
  </action>
  <verify>
    <automated>
      bash -n /tmp/infrahaus/infra/vm/templates/openclaw-desktop/template.conf &&
      python3 -c "import yaml; yaml.safe_load(open('/tmp/infrahaus/infra/vm/templates/openclaw-desktop/cloud-init/user-data.yaml'))" &&
      grep -q 'TEMPLATE_APP="OpenClaw Desktop"' /tmp/infrahaus/infra/vm/templates/openclaw-desktop/template.conf &&
      grep -q 'NOPASSWD:ALL' /tmp/infrahaus/infra/vm/templates/openclaw-desktop/cloud-init/user-data.yaml &&
      grep -q 'chage -d 0' /tmp/infrahaus/infra/vm/templates/openclaw-desktop/cloud-init/user-data.yaml &&
      ! grep -qE 'task-xfce-desktop|google-chrome|nodesource|tigervnc|openclaw' /tmp/infrahaus/infra/vm/templates/openclaw-desktop/cloud-init/user-data.yaml
    </automated>
  </verify>
  <done>
    - template.conf defines TEMPLATE_APP="OpenClaw Desktop" with desktop resources (4 CPU, 4096 RAM, 32G disk) and VM-specific fields (machine, bios, display)
    - cloud-init user-data.yaml is minimal: creates openclaw user with passwordless sudo, force-change password on first login, installs only bootstrap packages
    - cloud-init does NOT install XFCE, Chrome, Node.js, VNC, or OpenClaw
  </done>
</task>

<task type="auto">
  <name>Task 2: Create create-vm.sh wrapper script</name>
  <files>
    infra/vm/templates/openclaw-desktop/create-vm.sh
  </files>
  <action>
Create `infra/vm/templates/openclaw-desktop/create-vm.sh` as the entry point for creating an OpenClaw Desktop VM. Must be run as root on the Proxmox host.

Key design decisions (from CONTEXT.md):
- **VM stays running after creation** — do NOT run `qm template`. A running VM is immediately usable. Users can manually run `qm template <VMID>` later if they want a clone-able template.
- **bash -c subshell (not source)** — community VM scripts use whiptail interactive dialogs. Can't source them like LXC scripts. This means `$VMID` is NOT accessible from the subshell.
- **VMID detection via qm list** — after the community script completes, find the VM by searching `qm list` for `$TEMPLATE_HOSTNAME`. This is reliable because the community script sets the hostname we specify.

```bash
#!/usr/bin/env bash
# OpenClaw Desktop VM Creator
# Creates a Debian 13 VM with minimal cloud-init bootstrap.
# After creation, run ./run-scripts.sh <VM_IP> to install desktop software.
#
# Usage: bash create-vm.sh
# Must be run on the Proxmox host as root.

set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

# Load template defaults (all TEMPLATE_* vars, overridable via env)
source "${SCRIPT_DIR}/template.conf"

echo "============================================"
echo "  OpenClaw Desktop VM Creator"
echo "============================================"
echo ""
echo "Phase 1 — This script:"
echo "  Creates Debian 13 VM via ProxmoxVE community script"
echo "  Cloud-init: openclaw user, SSH, prerequisite packages"
echo ""
echo "Phase 2 — After VM boots (run separately):"
echo "  ./run-scripts.sh <VM_IP>  — installs full desktop"
echo "  Installs: XFCE, Chrome, Node.js 22, TigerVNC, OpenClaw"
echo ""
echo "Resources: ${TEMPLATE_CPU} cores, ${TEMPLATE_RAM}MB RAM, ${TEMPLATE_DISK} disk"
echo ""

# Pre-set env vars that the community script may read.
# NOTE: Community VM scripts use whiptail dialogs and may not honor all vars.
# Users can adjust values in the interactive dialog if these don't take effect.
export var_cpu="${TEMPLATE_CPU}"
export var_ram="${TEMPLATE_RAM}"
export DISK_SIZE="${TEMPLATE_DISK}"
export HN="${TEMPLATE_HOSTNAME}"
export START_VM="${TEMPLATE_START_VM}"

echo "Launching ProxmoxVE community script for base Debian 13 VM..."
echo "(Accept defaults or customize in the whiptail dialog)"
echo ""

# Run in bash -c subshell (intentional — community VM scripts are whiptail-interactive
# and cannot be sourced the way LXC scripts can. Tradeoff: $VMID is not accessible
# from the subshell, so we detect the created VM by hostname after this completes.)
bash -c "$(curl -fsSL https://raw.githubusercontent.com/community-scripts/ProxmoxVE/main/vm/debian-13-vm.sh)"

echo ""
echo "Community script finished. Detecting created VM by hostname..."

# Detect the VM created by the community script.
# We search qm list for our hostname since $VMID is not available from the subshell.
VMID=$(qm list | awk -v h="${TEMPLATE_HOSTNAME}" '$2 == h {print $1}' | tail -1)

if [[ -z "$VMID" ]]; then
  echo "ERROR: Could not find VM with hostname '${TEMPLATE_HOSTNAME}'."
  echo ""
  echo "Current VMs:"
  qm list
  echo ""
  echo "If the VM was created with a different hostname, run:"
  echo "  qm set <VMID> --name ${TEMPLATE_HOSTNAME}"
  echo "  Then re-run this script, or apply cloud-init manually."
  exit 1
fi

echo "Found VM: ${VMID} (${TEMPLATE_HOSTNAME})"

# Deploy cloud-init config to Proxmox snippets storage
SNIPPETS_DIR="/var/lib/vz/snippets"
mkdir -p "${SNIPPETS_DIR}"

CLOUD_INIT_SRC="${SCRIPT_DIR}/cloud-init/user-data.yaml"
SNIPPET_FILE="openclaw-desktop-${VMID}.yaml"

if [[ ! -f "${CLOUD_INIT_SRC}" ]]; then
  echo "ERROR: Cloud-init config not found at ${CLOUD_INIT_SRC}"
  exit 1
fi

cp "${CLOUD_INIT_SRC}" "${SNIPPETS_DIR}/${SNIPPET_FILE}"
echo "Cloud-init config -> ${SNIPPETS_DIR}/${SNIPPET_FILE}"

# Apply cloud-init and display settings to the VM
qm set "${VMID}" --cicustom "user=local:snippets/${SNIPPET_FILE}"
qm set "${VMID}" --vga "${TEMPLATE_DISPLAY}"
echo "Cloud-init and display settings applied."

# Restart VM to pick up cloud-init config (if already running from community script)
if qm status "${VMID}" | grep -q "running"; then
  echo "VM is running. Restarting to apply cloud-init..."
  qm shutdown "${VMID}" --timeout 30 2>/dev/null || qm stop "${VMID}"
  sleep 2
  qm start "${VMID}"
  echo "VM restarted."
elif [[ "${TEMPLATE_START_VM}" == "yes" ]]; then
  echo "Starting VM ${VMID}..."
  qm start "${VMID}"
fi

# NOTE: VM intentionally stays running. Do NOT convert to template via `qm template`.
# The VM is immediately usable. If cloning is needed later, the user can run:
#   qm template ${VMID}

echo ""
echo "============================================"
echo "  VM Created: ${VMID} (${TEMPLATE_HOSTNAME})"
echo "============================================"
echo ""
echo "Cloud-init is bootstrapping the VM (~2-3 minutes)."
echo "Monitor progress (from Proxmox host):"
echo "  qm agent ${VMID} exec -- cat /var/log/cloud-init-output.log"
echo ""
echo "When SSH is available, install desktop software:"
echo "  1. Get IP:  qm agent ${VMID} network-get-interfaces"
echo "  2. Run:     ./run-scripts.sh <VM_IP>"
echo ""
echo "Default credentials:"
echo "  SSH:      openclaw / openclaw  (must change on first login)"
echo "  VNC:      port 5901, password: openclaw  (set by scripts)"
echo "============================================"
```

Make executable: `chmod +x infra/vm/templates/openclaw-desktop/create-vm.sh`
  </action>
  <verify>
    <automated>
      bash -n /tmp/infrahaus/infra/vm/templates/openclaw-desktop/create-vm.sh &&
      test -x /tmp/infrahaus/infra/vm/templates/openclaw-desktop/create-vm.sh &&
      grep -q 'source.*template\.conf' /tmp/infrahaus/infra/vm/templates/openclaw-desktop/create-vm.sh &&
      grep -q 'debian-13-vm.sh' /tmp/infrahaus/infra/vm/templates/openclaw-desktop/create-vm.sh &&
      grep -q 'cicustom' /tmp/infrahaus/infra/vm/templates/openclaw-desktop/create-vm.sh &&
      ! grep -q 'qm template' /tmp/infrahaus/infra/vm/templates/openclaw-desktop/create-vm.sh
    </automated>
  </verify>
  <done>
    - create-vm.sh is executable and sources template.conf for resource defaults
    - Calls community Debian 13 VM script via bash -c subshell (not source)
    - Detects created VM by searching qm list for TEMPLATE_HOSTNAME (since subshell VMID is inaccessible)
    - Copies cloud-init yaml to /var/lib/vz/snippets/ and applies via qm set --cicustom
    - VM remains running after creation (no qm template conversion)
    - Output guides user to check cloud-init progress and run ./run-scripts.sh next
  </done>
</task>

</tasks>

<verification>
- Directory exists: `ls -la /tmp/infrahaus/infra/vm/templates/openclaw-desktop/`
- Syntax checks pass: `bash -n /tmp/infrahaus/infra/vm/templates/openclaw-desktop/create-vm.sh && bash -n /tmp/infrahaus/infra/vm/templates/openclaw-desktop/template.conf`
- Cloud-init YAML is valid Python yaml.safe_load
- template.conf has all TEMPLATE_* variables including VM-specific ones (MACHINE, BIOS, DISPLAY)
- create-vm.sh does NOT contain `qm template` (VM stays running)
- cloud-init does NOT reference any desktop packages
</verification>

<success_criteria>
- infra/vm/templates/openclaw-desktop/ exists with template.conf, create-vm.sh, cloud-init/user-data.yaml
- template.conf mirrors LXC template pattern with VM-specific additions (machine, bios, display, cloud-init)
- cloud-init is MINIMAL: creates openclaw user with passwordless sudo, force-change password, bootstrap packages only
- create-vm.sh wraps community script, applies cloud-init, and leaves VM running (no qm template)
- Post-creation output guides user to run-scripts.sh for desktop software installation
</success_criteria>

<output>
After completion, create `.planning/phases/07-vm-to-run-openclaw/07-01-SUMMARY.md`
</output>
