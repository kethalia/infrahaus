---
phase: 04.5-auth-decoupling
plan: 02
subsystem: auth
tags: [siwe, viem, eip-1271, iron-session, redis, api-routes, lukso]

# Dependency graph
requires:
  - phase: 04.5-01
    provides: "Rewritten session.ts with SIWE types, luksoPublicClient, viem/wagmi deps"
provides:
  - "SIWE nonce generation endpoint (GET /api/auth/nonce)"
  - "SIWE signature verification endpoint (POST /api/auth/verify)"
  - "Session destruction endpoint (POST /api/auth/logout)"
  - "Session status check endpoint (GET /api/auth/me)"
  - "Simplified auth/actions.ts with only logoutAction"
affects: [04.5-03, 04.5-04]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "SIWE auth flow: nonce → sign → verify → session"
    - "Iron-session nonce cookie for CSRF-safe SIWE verification"
    - "EIP-1271 smart contract signature verification via verifySiweMessage"

key-files:
  created:
    - "apps/dashboard/src/app/api/auth/nonce/route.ts"
    - "apps/dashboard/src/app/api/auth/verify/route.ts"
    - "apps/dashboard/src/app/api/auth/logout/route.ts"
    - "apps/dashboard/src/app/api/auth/me/route.ts"
  modified:
    - "apps/dashboard/src/lib/auth/actions.ts"

key-decisions:
  - "Nonce stored in iron-session cookie (not Redis) — temporary, consumed by verify"
  - "verifySiweMessage used for EIP-1271 smart contract account support"
  - "expiresAt defaults to 2h from now if SIWE message has no expirationTime"

patterns-established:
  - "SIWE auth API: nonce → verify → session creation flow"
  - "RainbowKit-compatible auth endpoints at /api/auth/*"

# Metrics
duration: 3min
completed: 2026-02-23
---

# Phase 4.5 Plan 2: SIWE Auth API Routes Summary

**Four SIWE authentication API routes with EIP-1271 smart contract verification via viem/lukso and simplified auth/actions.ts**

## Performance

- **Duration:** 3 min
- **Started:** 2026-02-23T20:15:22Z
- **Completed:** 2026-02-23T20:18:33Z
- **Tasks:** 2
- **Files modified:** 5

## Accomplishments

- Created complete SIWE auth flow: nonce generation, signature verification, session management, and status check
- POST /api/auth/verify uses `luksoPublicClient.verifySiweMessage()` for automatic EIP-1271 support with Universal Profile smart contracts
- Removed all Proxmox login logic from auth/actions.ts (171 → 14 lines), keeping only logoutAction
- Middleware already allows /api/auth/* routes without session (confirmed)

## Task Commits

Each task was committed atomically:

1. **Task 1: Create SIWE authentication API routes** - `721bbb6` (feat)
2. **Task 2: Simplify auth/actions.ts** - `65e8109` (feat)

## Files Created/Modified

- `apps/dashboard/src/app/api/auth/nonce/route.ts` - GET handler: generates SIWE nonce, stores in iron-session cookie
- `apps/dashboard/src/app/api/auth/verify/route.ts` - POST handler: validates SIWE signature (EIP-1271), creates Redis session
- `apps/dashboard/src/app/api/auth/logout/route.ts` - POST handler: destroys Redis session and iron-session cookie
- `apps/dashboard/src/app/api/auth/me/route.ts` - GET handler: returns current session address or null
- `apps/dashboard/src/lib/auth/actions.ts` - Simplified to logoutAction only (removed loginAction, loginSchema, ensureNodeExists)

## Decisions Made

- Nonce stored in iron-session cookie (not Redis) — it's temporary and consumed by /api/auth/verify
- Used `luksoPublicClient.verifySiweMessage()` which automatically handles EIP-1271 for UP smart contract accounts
- Default expiresAt is 2 hours from now when SIWE message has no expirationTime field
- Kept logoutAction using authActionClient pattern — sidebar logout still uses server action

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- All four SIWE API routes ready for RainbowKit auth adapter integration (plan 04.5-04)
- auth/actions.ts cleaned — logoutAction is the only export
- Plan 04.5-03 (Proxmox client refactor + identity migration) can proceed
- Note: unstaged page.tsx change exists from plan 01/03 (session.username → session.address) — will be handled in subsequent plans

## Self-Check: PASSED

---
*Phase: 04.5-auth-decoupling*
*Completed: 2026-02-23*
